class Chart {
    constructor(options){
        this.element = options.element;
        this.element = options.widht;
        this.element = options.height;
        this.element = options.margin;
        this.element = options.padding;
        this.element = options.data;

        this.cleanData(this.data);
        this.marginConvention();
        this.scales();
        this.axes();
    }

    cleanData(data){

    }
    marginConvention(){

    }
    scales(){

    }
    axes(){
        
    }
}